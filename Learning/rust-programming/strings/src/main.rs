/*
    Strings in Rust

    A String is a piece of text or, alternatively, a sequence of characters.

    &str -> String slice (reference/borrowed/immutable)

    String -> lives on Heap (mutable/ownership)
    - let empty: String = String::new();
    - let temp: String = String::from("Rust!");
*/

fn main() {
    println!("\nStrings in Rust!\n");
}
